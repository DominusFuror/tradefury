# Инструкция по запуску WoW WotLK Crafting Monitor

## 🚀 Быстрый запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск в режиме разработки

**Важно:** Приложение требует запуск двух процессов:

#### Терминал 1: Node.js сервер (хранилище данных)
```bash
npm run server
```
Сервер запустится на порту **3001** и будет обслуживать API для хранения данных.

#### Терминал 2: React приложение
```bash
npm start
```
Приложение будет доступно по адресу: **http://localhost:3000**

> **Примечание:** React автоматически проксирует все запросы к `/api` на порт 3001, поэтому оба процесса должны быть запущены.

### 3. Сборка для продакшена

```bash
npm run build
```

Результат сборки будет в папке `build/`.

## 🔧 Переменные окружения (опционально)

Создайте файл `.env` в корне проекта для настройки:

```env
# API URL (не обязательно в dev mode благодаря proxy)
REACT_APP_API_URL=/api

# Wowhead API
REACT_APP_WOWHEAD_API_URL=https://www.wowhead.com/wotlk/api

# Debug mode
REACT_APP_DEBUG=true

# Server configuration (для production)
PORT=3001
DATA_DIR=./server/data
CORS_ORIGIN=*
BODY_LIMIT=5mb
```

## 📱 Использование

1. **Загрузите данные Auctionator**
   - Скопируйте файл `Auctionator.lua` из WoW (обычно в `WTF/Account/<account>/SavedVariables/`)
   - Загрузите файл через панель "Auctionator Data" в приложении

2. **Выберите профессию**
   - Выберите профессию для анализа крафтов

3. **Анализируйте крафты**
   - Просматривайте список крафтов с расчетом профита
   - Используйте фильтры и сортировку для поиска выгодных крафтов
   - Нажмите кнопку "Обновить" чтобы загрузить свежие данные с сервера

4. **Детальный анализ**
   - Кликайте на крафты для просмотра детальной информации о материалах

## 🎯 Основные функции

- ✅ Загрузка и парсинг данных Auctionator
- ✅ Сохранение истории цен на сервере
- ✅ Общий доступ к данным для всех пользователей
- ✅ Мониторинг всех профессий WotLK
- ✅ Расчет профита и ROI
- ✅ Фильтрация и сортировка крафтов
- ✅ Адаптивный дизайн
- ✅ Обновление данных по кнопке
- ✅ Логирование загрузок на сервере

## 🔮 Development vs Production

### Development (два процесса)
- `npm run server` - запускает API сервер на :3001
- `npm start` - запускает React dev server на :3000 с proxy

### Production (один процесс)
- Сервер обслуживает и API и статические файлы React на одном порту
- Требует настройку среды выполнения для продакшена

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.
